{{>partials/header}}

{{#banner}}
<section class="banner" style="background-image: url('{{ background }}')">{{ title }}</section>
<h2>{{ title }} disponíveis para adoção</h2>
{{/banner}}

  <div class="container list">
    {{#pets}}
      <div class="item">
        <img src="{{ image }}" class="item--image" />
        <div class="item--name">{{ name }}</div>
        <div class="item--color">Cor: {{ color }}</div>
        <div class="item--genre">Gênero: {{ sex }}</div>
      </div>
    {{/pets}}
  </div>

  {{^pets}}
    <div class="container no-list">
      <h2>Nenhum animal foi encontrado 😓</h2>
    </div>
  {{/pets}}

{{>partials/footer}}